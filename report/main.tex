\documentclass[11pt]{article}
\usepackage[margin=1.2in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{fourier}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{mathtools}
\usepackage{latexsym}
\usepackage{graphicx}
\usepackage{float}
\usepackage{etoolbox}
\usepackage{hyperref}
\usepackage{tikz}
\usepackage{booktabs}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18} 
\usepackage{lipsum}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{nccmath}
\usepackage{eucal}
\usepackage{todonotes}
\usepackage[most]{tcolorbox}
\usepackage{wrapfig}
\newtcolorbox[auto counter]{problem}[1][]{%
    enhanced,
    breakable,
    colback=white,
    colbacktitle=white,
    coltitle=black,
    fonttitle=\bfseries,
    boxrule=.6pt,
    titlerule=.2pt,
    toptitle=3pt,
    bottomtitle=3pt,
    title=GitHub repository of this project}

\RequirePackage[activate={true,nocompatibility},final,tracking=true,kerning=true,spacing=true]{microtype}
\SetTracking{encoding={*}, shape=sc}{40} % Spacing for Small Caps


\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\V}{\mathcal{V}}
\newcommand{\E}{\mathcal{E}}
\newcommand{\G}{\mathcal{G}}
\renewcommand{\L}{\mathcal{L}}

\newcommand{\defeq}{\vcentcolon=}
\newcommand{\eqdef}{=\vcentcolon}
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\newcommand{\Kr}[3]{\mathcal{K}_{#1}(#2,#3)}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\title{%
    Accelerated Filtering on Graphs using Lanczos method
    \\ \large Scientific Computing project report}
\author{Alberto Defendi}

\date{}

\setlength{\parskip}{1em}
\setlength{\parindent}{0em}

\begin{document}
\maketitle

\begin{abstract}
    \noindent Signal processing on graph is becoming . We develop an efficient implementation of the Lanczos
    method that performs full orthogonalization when loss of orthogonality is detected. We discuss
    and test \ldots
\end{abstract}

{\setlength{\parskip}{0em}
\tableofcontents}

\section{Introduction}

We introduce basic graph theory concepts and briefly overview the results used in the project experiments.

\subsection{Signal processing on graphs}



We consider a weighted undirected graph $ \G = (\V, \E, \mathcal{W}) $,
where $ \V \subset \R^N $ is the set of vertices, $ \E \subset \R^M $ is the set of edges, and $ \mathcal{W}
: \V \times \mathcal{V} \to \R$ is a weight function. The weight function can be
represented with a $ N \times N $ matrix $ W $ such that 
$W_{i,j} = 
\begin{cases} 
    W(v_i, v_j), & \text{if } (v_i, v_j) \in \E \\ 
    0, & \text{otherwise} 
\end{cases} 
\quad \text{for all } i,j = 1, \dots, |\V|
$,
and for our needs, we assume $ W $ to be symmetric, that is $ W_{i,j} =  W_{j,i} $.


\todo{Remove one legend, make tighter}
\begin{wrapfigure}{r}{8cm}
  \caption{Filtering signal on graphs.} 
  \includegraphics[width=8cm]{assets/printed_graphs.pdf}
\end{wrapfigure}

In the study of signal processing on graph, we model the idea of sending a signal to a node as
assigning a value to a vertex with a function $ s : \V \to \R$, whose values can be represented as a vector
$ s = s(\V) = [s_1, \dots, s_N] \in \R^N $ where each entry $ s_i \defeq s(v_i)$ represents
the signal sent over a node $ v_i \in \V$. We also keep track of the weight of each vertex
with a function $ d(i) \defeq \sum_{j=1}^N W_{i,j} $, that we represent with the matrix $D =
\text{diag}(d(1), \dots, d(N))$.

In this setting, we introduce the graph Laplacian $\L$ defined as 
$ \L = D - W$, where $D $ is the diagonal degree matrix with entries  $D_{ii} = d(i)$. By
construction, $\L^T = \mathcal{L}$, thus the Lanczos method can be safely applied to our 
problem.



\begin{remark}
  The Laplacian $\L$ is symmetric and semi-definite (it is diagonally dominant), hence by the
  spectral theorem it admits the decomposition
  \begin{equation*}
    \L = U \Lambda U^{*},
  \end{equation*}
  where $ U = \left[ u_0,\dots,u_{N-1} \right] \in O(N)$ is called Fourier basis, and $ \Lambda =
  \text{diag}\left(\lambda_0, \dots, \lambda_{N-1}\right)$ is the matrix of eigenvalues of $ \L $.
  Without loss of generality we can assume $ 0 =\lambda_0 \leq \lambda_1  \dots \leq \lambda_{N-1} $
  and the vectors in $ U $ to be in the same order that the eigenvalues.
  \end{remark}

\begin{definition}[Graph signal]
  A graph signal is a continuous function $ g : \R^+ \to \R $. For our needs, we will assume $ g $ to be
  defined on the spectrum of $ \L $.
\end{definition}
Diagonalising the Laplacian would give an easy way to compute the function $ g(\L) $ by
evaluating it on the eigenvalues of $ \L $, which means computing $ g(\L) = U g(\Lambda) U^{*} $.
In matrix notation, applying a filter to a graph $ \G $ corresponds to the operation
\begin{equation*}
  s^\prime \defeq g(\L)s = U g(\L) U^{*}s.
\end{equation*}
\begin{remark}[Computational cost]
Computing the Fourier basis for $ \L $ is computationally expensive for large graphs, thus the
choice of using the Lanczos method for $ g(\L) $, with its computational cost of $ O(M \dot |\E|) =
O(M \dot N)$ \todo{CHECK and move computational cost of lanczos (all orgthogs) up}, it offers an efficient alternative in computing $ g(\L) $. However, storing the basis $ V_M $ costs MN
additional memory, that could be avoided using a two-step implementation, that we leave for future
work.
\end{remark}

In this work, we will restrict ourself to two graph models: Erdős-Rényi and Sensor graphs. We shall
define them as the current implementation in Library \cite{pygsp} 
\begin{definition}[Erdős-Rényi graph]
  \label{def:ER}
  We define the Erdős-Rényi graph $ \G_{N,p} $ with $ N $ nodes, and probability $ p $, as the graph
  constructed by randomly connecting two nodes independently from any other edge, with probability $ p $.
\end{definition}
\begin{definition}[Sensor graph]
  \label{def:Sensor}
  A sensor graph $ \G_N $ is the graph constructed by picking $ N $ points on the square $ \left[
  0,1 \right] \times \left[ 0,1 \right] $ and connecting each point with edges using $ k $ nearest
  neighbors.
\end{definition}
\begin{remark}
  As currently stating in \href{https://github.com/epfl-lts2/pygsp/blob/4716b12c470c579e3f477f2d1c1d99752c73252d/pygsp/graphs/nngraphs/sensor.py#L6}{PyGSP
    sensor graph source code}, definition of this graph changed in 2019, thus our used sensor graph
    might give slightly different results than the 2015's article \cite{susnjara2015} we are trying
    to reproduce. We don't investigate this difference as it is unclear how the new library improves
    the old one.
  
\end{remark}

AD Laplacian properties


\subsection{The Lanczos method}

\begin{definition}[Krylov subspace]
 Given a matrix $ A \in \R^{N \times N} $ and a vector $ b \in R^N $, the Krylov subspace of order
 $j$ is defined as the set $ \mathcal{K}_j (A,b) = \text{Span} \left(b, Ab, A^2b, \dots,
A^{j-1}b\right)  $. We represent the basis of this subspace with a matrix $ V_M = \left[ v_1,\dots,v_M
\right] \in \R^{N \times M}$.
\end{definition}
From the Arnoldi relation
\begin{equation*}
  AV_j = V_jH_j + h_{j+1,j}v_{j+1}e_j^{*}, \\ \hspace{20pt}
  H_j = V_j^{*}AV_j =
  \begin{bmatrix}
    h_{11} & \dots & \dots & h_{1,j} \\
    h_{21} & h_{22} & & \vdots \\
           & \ddots & \ddots & \vdots \\
           & & h_{j,j-1} & h_{j,j}.
  \end{bmatrix},
\end{equation*}
we can derive the Lanczos method for a symmetric, positively defined matrix $ A $. Setting
\begin{equation*}
\alpha_j \defeq h_{j,j},\hspace{20pt}\beta_j \defeq h_{j+1,j}.
\end{equation*}
From our hypothesis on $ A $, it follows that the matrix $ H_j $, which is both upper and lower Hessenberg 
matrix, is also symmetric; in other words, it is tridiagonal—thus we rename it $ T_j $. Hence
the Arnoldi relation takes the form:

\begin{equation*}
  AV_j = V_j \alpha_j  + \beta_j v_{j+1}e_j^{T}, \\ \hspace{20pt}
  T_j = V_j^{\top} A V_j = \begin{bmatrix}
    \alpha_1 & \beta_1 \\
    \beta_1 & \ddots & \ddots \\
      & \ddots & \ddots & \beta_{j-1} \\
      & & \beta_{j-1} & \alpha_j
  \end{bmatrix}.
\end{equation*}.

\begin{proposition}
 Consider the filter $ g : [0, \lambda_{\text{max}}] \to \R$ and a signal vector $ s \in \R^N $.
 Then the Lanczos method approximates $ g(\L)s $ with: \footnote{This results holds in general for any function $f :\Omega \to \C$, where $ \Omega \subset \Lambda(A) $.}
\begin{equation}
 g(\L)s \approx  V_M \norm{s}_2 g(T_M) e_1 \eqdef g_M. \label{eq:projection}
\end{equation}
\end{proposition}
Numerically, we compute the eigendecomposition $ (\Lambda,U) $ from $ T $, and then project $ g(T_M) = Ug(\Lambda)U^{\top} $ using the equation above.


\subsection{Algorithm}
\label{ssec:algo}

In floating point arithmetic the basis $ V $ stops being orthogonal, and orthogonalizing
the incoming vector $ w $ only against $ v_j,v_{j-1} $ is not sufficient anymore: we need to apply
this process with all the previous basis vectors. To do so, we implement Algorithm \ref{alg:lanczos}, 
following \cite{susnjara2015} and \cite{demmel1997} with these criteria: full reorthogonalization is triggered if loss of
orthogonality is detected after calling Lanczos without orthogonalization, that is when $ V_M $ is
weakly orthogonal, that is $ \|V_M^{\top} V_M
- I \|_2 > \varepsilon $ for a chosen threshold $ \varepsilon = 10^{-10} $. When the algorithm is called
passing a given threshold $ \varepsilon $, it computes the residual $
\|r_j \|_2  = \| g_j - g_{j-3}\|$, which computes $ g $ using Equation \ref{eq:projection}.
\\
 $  g_M \defeq \|b \|_2V_Mg(T_M)e_1
$, where $ g(T_M) $ is computed by diagonalizing $ T_M $: because $ T_M $ is symmetric and tridiagonal, 
computing its eigendecomposition is stable and efficient. \todo{why? cost?}


\begin{algorithm}[ht!]
  \caption{Modified Lanczos method in floating point arithmetic.}
  \label{alg:lanczos}
  \begin{algorithmic}
    \State \textbf{Input:} Symmetric matrix $ A \in \R^{N\times N}$, vector $ b \in R^N $, $ M \in
    \N $, and $ g $ function.
    \State \textbf{Output: } Basis matrix $ V_l = \left[ v_1,\dots,v_l \right] \in R^{N \times l} $ for $ K_l(A,b) $, vectors $
    \alpha =\left\{ \alpha_1,\dots,\alpha_l \right\} \in \R^l, \beta=\left[ \beta_1,\dots,\beta_{l-1} \right] \in \R^{l-1} $, where $ l \in
    \{ 1,\dots,M \}$ is the stopping iteration index.
    \State Initialize $ V \leftarrow \mathbf{0}^{N \times M} $ , $ \alpha \leftarrow \mathbf{0}^N $,
    and $ \beta \leftarrow \mathbf{0}^{N-1}$

    \For{$ j = 1, \dots, M-1 $}
    \State $ w \leftarrow Av_j  $
    \State $ \alpha_j \leftarrow v_j^{\top} w $

    \State $\left\{
    \begin{array}{ll}
        w \leftarrow w - \alpha_j v_j&  \text{No reorthogonalization} \\
        \text{\textbf{if} } j>0 \text{ \textbf{then} } w \leftarrow w - \beta_j v_{j-1}\text{ \textbf{end
    if}} & \\ \\
        w = w - \sum_{i=1}^j (w^{\top}v_i)v_i & \text{Full reorthogonalization} \\
        w = w - \sum_{i=1}^j (w^{\top}v_i)v_i 
    \end{array}
    \right.$
    \If{$ j < M $}
    \State $ \beta_j = \|w\|_2 $
    \If{$ \beta_j = 0 $}
      \State 
      \Return $ V_j, \alpha_{1:j}, \beta_{1:j-1} $
      \Comment{--- Breakdown ---}
    \Else
      \If{$ \| g_j - g_{j-3}\|_2  < \varepsilon$}
      \State 
      \Return $ V_j, \alpha, \beta $
      \Comment{--- Breakdown ---}
      \EndIf
      \State $ v_{j+1} = \frac{w}{\beta_j} $
    \EndIf
    \EndIf
    \EndFor
    \State \Return $ V_M, \alpha_{1:M}, \beta_{1:M-1} $.
  \end{algorithmic}
  
\end{algorithm}

\begin{algorithm}[ht!]
  \caption{Approximation of $ g(\L)s $ using Lanczos.}
  \label{alg:g_evaluation}
  \begin{algorithmic}
    \State \textbf{Input:} Function $ g $, Matrix $ T $, signal $ s $.
    \State $ \lambda, U \leftarrow $ diagonalize($ T $)  \Comment{Here $ \lambda $ is the vector of
    eigenvalues.}
    \State $ u_1 \leftarrow  (U^{\top})_{:,1}$
    \State $y \leftarrow \|s\| U (\text{Diag}(g(\lambda)) U^{\top})e_1 = \|s\| U (g(\lambda) \odot u_1)$
    \State \Comment{Here $ u \odot v $ denotes the element-wise product between
  two vectors $ u $ and $ v $.}
    \State \Return $ Vy $
  \end{algorithmic}
\end{algorithm}

\begin{algorithm}[ht!]
  \caption{Evaluation of $ g(\L)s $ using Fourier basis.}
  \label{alg:g_evaluation_fourier}
  \begin{algorithmic}
    \State \textbf{Input:} Function $ g $, Graph $ \G $, signal $ s $.
    \State $ \Lambda,U \leftarrow \text{Fourier\_basis}( \G )$
    \State \Return $ U g(\Lambda) U^{\top}s $
  \end{algorithmic}
  
\end{algorithm}


\begin{remark}[Computational cost]
  Lanczos algorithm computes $ V $  with partial orthogonalization, with $ O(M \cdot N
  + M \cdot \text{nnz}(A)) $ for a sparse matrix \footnote{note that the product $ Av_j $ dominates the cost,
  and that $ \text{nnz}(A) = N^2 $ when $ A $ is dense.}. With full orthogonalization, computing $ w = w -
  \sum_{i=1}^j v_i(v_i^{\top}w) $ can cost up
  to $ O(M\cdot N) $, thus the total cost becomes $ O(M^2N +  M\cdot \text{nnz}(A)) $. \todo{cost of
  computing STEP ER} While it is an efficient
  method for small $ M\ll N $ for computing the basis of a Krylov subspace, it has a significant $ O(M \cdot N) $ memory cost for storing $ V_M $.
\end{remark}


\subsubsection{Why double orthogonalization?}

  From the experimental setting in Section \ref{ssec:ex1}, we observed that the orthogonality of the
  basis $ V $ with Arnoldi was worse than Lanczos basis. Double orthogonalization showed good
  results. One reason could be that the Laplacian matrices that we are considering are badly
  conditioned. The following example motivates this choice:
\begin{example}
    
We consider a ER graph with $ N = 1000,M=200, p=0.04 $.
The associated Laplacian $ \L $ is terribely conditioned, indeed $ k_2(\L) = 2.365029616834158 \times 10^{17} $:
  \input{assets/orthogonalization.tex}

  \end{example}
  As we can observe in Table \ref{table:orthog}, orthogonalizing two times against the previous
  basis vectors, yelds to a better orthogonal basis $ V$, while a single iteration here performs worse than standard Lanczos.


\subsubsection{Comparing Lanczos with Arnoldi}

Here we do a short comparison of Lanczos and Arnoldi with double orthogonalization. From the theory,
we know that the former is faster than the latter, but wehaven't seen a practical comparison in
class.

We first introduce the Arnoldi algorithm with double orthogonalization in Algorithm \ref{alg:arnoldi_double_ortho}, as seen in class.

\begin{algorithm}[ht!]
  \caption{Arnoldi with double orthogonalization.}
  \label{alg:arnoldi_double_ortho}
  \begin{algorithmic}[1]
    \State \textbf{Input:} Matrix $ A $, starting vector $ s $, maximum dimension $ M $, function $
    g $, tolerance $ \varepsilon $.
    \State $ v_1 \leftarrow s / \|s\|_2 $
    \State Initialize $ V \leftarrow \mathbf{0}^{N \times (M+1)} $ and $ H \leftarrow \mathbf{0}^{(M+1) \times M} $
    \State $ V_{:, 1} \leftarrow v_1 $
    \For{$ j = 1 \dots M $}
        \State $ w_1 \leftarrow A v_j $
        \State \Comment{First orthogonalization}
        \State $ h_1 \leftarrow V_j^\top w_1 $
        \State $ w_1 \leftarrow w_1 - V_j \cdot h_1 $
        \State $ v_{j+1} \leftarrow w_1 / \|w_1\|_2 $ 
        
        \State \Comment{Second orthogonalization pass}
        \State $ h_2 \leftarrow V_j^\top v_{j+1} $
        \State $ w_2 \leftarrow v_{j+1} - V_j \cdot h_2 $
        \State $ H_{1:j, j} \leftarrow h_1 + h_2 $
        \State $ H_{j+1, j} \leftarrow \|w_1\|_2 \|w_2\|_2 $\\
        
        \If{$ H_{j+1, j} = 0 $}
            \State \textbf{break} \Comment{Arnoldi breakdown}
        \EndIf
        
        \State \Comment{Convergence check}
        \If{$ j > 3 $ \textbf{and} $ j < M $}
            \If{$ \|g_j - g_{j-3}\|_2 < \varepsilon $}
                \State \Return $ V_j, H_{1:j, 1:j}, j $
            \EndIf
        \EndIf
        
        \State $ v_{j+1} \leftarrow w_2 / \|w_2|_2 $
        \State $ V_{:, j+1} \leftarrow v_{j+1} $
    \EndFor
    
    \State Set elements of $ H $ with magnitude less than machine epsilon to $ 0 $
    \State \Return $ V_{:, 1:j}, H_{1:j, 1:j}, j $
  \end{algorithmic}
\end{algorithm}

We consider again function the function $ g $ used in the experiments and $ N \in \left\{ 300, 400,
500, \dots, 3000\right\} $, $ M = 200 $ and $ p = 0.04 $. In Figure \ref{fig:avl} Lanczos
outperforms Arnoldi, but curves follow a similar behaviour.

\begin{figure}[ht!]
  \centering
  \caption{Comparing Lanczos and Arnoldi, both with double orthogonalization.} 
  \includegraphics[width=0.8\textwidth]{assets/avl.pdf}
  \label{fig:avl}
\end{figure}

In our implementation we tested that the matrix $ H $ returned by Arnoldi is tridiagonal (up to some small perturbations outside the diagonals) and
symmetric (up to a small epsilon).

\section{Experiments}

In the following section we generate random Erdős-Rényi and Sensor graphs, as defined in Definitions
\ref{def:ER} and \ref{def:Sensor}, to study Lanczos algorithm's accuracy and performance.

\subsection{Comparing global error with projection error}
\label{ssec:ex1}

Scope of this experiment is to numerically verify the approximation in Equation
\eqref{eq:projection}. We will do it by relating the following residuals, which represent the
algorithmic error and the ground truth $ g(\L) $.
Let's recall some quantities that will be useful in this section: $ N $ is the number of graph
nodes and size of the Laplacian $ \L $, $ M $ is the maximum size of the Krylov subspace, and $ p $
the probability of the graph. In the following experiment we set $ (N,M,p) = (500,200,0.04) $.

\begin{definition}(Errors)
  We define the Lanczos \emph{iteration error} as $\norm{g_{M+j} - g_M}_2 $, where $ j $ is small,
  and the \emph{true error} as $ \norm{e_M}_2 = \norm{g(\L)s - g_M}_2 $.
\end{definition}

Computing $ g(\L)s $ is done by computing the Fourier basis \footnote{In other
words diagonalizing the laplacian} for the graph $ \G $, which feasible for the small graph we are
considering. We try to replicate the Experiment 1 in Article \cite{susnjara2015}. As filterbank \todo{Definition?}, we consider the function $ g(t)
= \sin(\frac{1}{2}\pi \cos^2(\pi t)) \chi_{\left[ -\frac{1}{2}, \frac{1}{2} \right]}$. We adapt this
function to the graph spectrum, using the \texttt{Itersine} class in Pygsp, which we plot in Figure
\ref{fig:itersine}. Sadly, the autors in the paper apply another transformation to this function
called \emph{filter warp}, which we weren't able to reproduce as it was not documented, both in the article and library.

\begin{figure}[ht!]
  \centering
  \caption{Itersine plot.} 
  \includegraphics[width=1.0\textwidth]{assets/itersine_comparison.pdf}
  \label{fig:itersine}
\end{figure}

Using the algorithm described in Section \ref{ssec:algo}, and computing the errors for $ j = 1\ldots
M $, we obtain the plot in Figure \ref{fig:ex1_estimate}:

\begin{figure}[ht!]
  \centering
  \includegraphics[width=1.0\textwidth]{assets/ex1_estimate.pdf}
  \caption{Comparing iteration error with true error.}
  \label{fig:ex1_estimate}
\end{figure}

Let's analyze Figure \ref{fig:ex1_estimate}: we immediately observe that the error curves are
closely tight, and that their value is decreasing, which means that the approximation gains
precision as the basis $ V $ gets larger. We also see how the eigenvalues of $ \L $ and $ T $
influence the steepness and smoothness of the curves. Indeed, when eigenvalues fall outside $
[-\frac{1}{2}, \frac{1}{2}] $, we see that the curves fastly reach machine precision: this is
unfortunately caused by the itersine not perfectly matching the spectrum distribution. We also
observe evident lack of smoothness in the iteration error curve. We believe that due the
irregularity of $ g(t) $ in $ t \in \{  -\frac{1}{2}, \frac{1}{2} \}$, the eigenvalues of $ T $ tend
to jump in and out the interval $ [-\frac{1}{2}, \frac{1}{2}] $, causing spikes.
\todo{I'm not really sure about this.}

\subsection{Performance comparison for Erdős-Rényi graphs}

In this experiemnt, we will study the performance correlation for running Lanczos on Erdős-Rényi
graphs $ \G(N,p) $, comparing time data when only $ N $ varies and when only $ p $ varies
\todo{What's the motivation for this test?}.
We choose $ \varepsilon = 10^{-5} $, as we observed that smaller thresholds rarely triggered full
orthogonalization. As described in Algorithm \ref{alg:lanczos} our stopping criteria happens when $ \|g_j - g_{j-3}\|_2 < \epsilon $, at the step $
j$. \footnote{Here
index goes backwards because matrix $ T_l $ would not be defined for $ l>j $ at iteration $ j $!}
In the following experiment, we choose generate two sample datasets, letting $ n = 6 $,:
\begin{enumerate}
  \item $ N $ varies in $ \{ 50,100,\dots,2^n \cdot 50\} $, $ p = 0.04 $.
  \item $ p $ varies in $ \{ 0.01,0.02,\dots,2^n \cdot 0.01 \}$, $ N = 1000$.
\end{enumerate}
We repeat the generation of these graphs $ 400 $ times to avoid graph randomness biasing our results.
For time telemetry, we adopt Python's \texttt{time.perf\_monitor()} which ensures precise
measurement. When measuring time, first the algorithm runs without orthogonalization, and if full
orthogonalization is triggered, it is taken in the total measurement. From Table
\ref{table:ex23_some} shows that for some cases, we can't just blindlessy enforce full
orthogonalization, but we run it when we detect orthogonality loss.
\begin{table}[ht!]
  \centering
  \caption{Lanczos execution tests when $ N $ varies and $ p $ fixed (top), and 
  vice versa (bottom).}
  \label{table:ex23_some}
  \input{assets/table_ex_2.tex}
  \input{assets/table_ex_3.tex}
\end{table}

After executing the experiments above, we obtain the plot in Figure \ref{fig:loglog}.
\todo{CHANGE FIG AND FIX wrong figure numbers.}

\begin{figure}[ht!]
  \centering
  \caption{Log-log plot of four iteration of the results in Table AVG TABLE} 
  \label{fig:loglog}
  \includegraphics[width=0.6\textwidth]{assets/correlation_23.pdf}
\end{figure}


\section{Conclusions \& future work}

We did't address the high memory cost of $ O(N\cdot M) $ of Lanczos, this could be avoided by
using a two-step implementation, that stores only three vectors and reconstructs the basis when
needed. Further, because full orthogonalization was widely used in the second experiment, we could
implement selective reorthogonalization as described in \cite{demmel1997}, but both methods fall
behind the objective of this course.

\clearpage
\bibliographystyle{unsrt}
\bibliography{ref}
\nocite{*}



\end{document}
